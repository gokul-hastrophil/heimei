"""CLI subcommands for the Heimei Typer app.

Each module here exposes a `Typer` app named `app`, added to the root
app in `heimei.main` under a matching subcommand name (e.g.
`heimei.cli.doctor:app` becomes `heimei doctor`). See `README.md` for
the full command reference.
"""
