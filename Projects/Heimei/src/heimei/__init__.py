"""Heimei — Personal AI Operating Layer.

The CLI entry point is `heimei.main:app` (see `pyproject.toml`'s
`[project.scripts]`). See `README.md` for usage, `docs/ARCHITECTURE.md`
for how the subsystems fit together, and `docs/ROADMAP.md` for what's
planned. `config`, `core`, `logging`, `inventory`, and `doctor` are
implemented; every other package under `heimei.*` is reserved for
future work and not yet implemented.
"""
